// packages/api-contracts/src/index.ts
// Placeholder export surface for the future API contracts package.
// Establishes the package boundary early so OpenAPI work can land without reorganizing the repo.
export const apiContractsWorkspaceStatus = {
  packageName: "@etest/api-contracts",
  status: "ready for import and review contracts",
} as const;
